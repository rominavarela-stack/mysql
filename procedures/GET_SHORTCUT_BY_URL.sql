DROP PROCEDURE IF EXISTS urls.GET_SHORTCUT_BY_URL;

DELIMITER //

CREATE PROCEDURE urls.GET_SHORTCUT_BY_URL
(
    IN url VARCHAR(2000)
)

BEGIN

SELECT id, url
    FROM urls.SHORTCUT
    WHERE SHORTCUT.url=url;

END //

DELIMITER ;
