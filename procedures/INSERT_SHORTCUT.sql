DROP PROCEDURE IF EXISTS urls.INSERT_SHORTCUT;

DELIMITER //

CREATE PROCEDURE urls.INSERT_SHORTCUT
(
    IN url VARCHAR(2000)
)

BEGIN

INSERT INTO urls.SHORTCUT ( url )
    VALUES ( url );

SELECT id, url
    FROM urls.SHORTCUT
    WHERE SHORTCUT.url=url;

END //

DELIMITER ;
