DROP PROCEDURE IF EXISTS urls.GET_SHORTCUT_BY_ID;

DELIMITER //

CREATE PROCEDURE urls.GET_SHORTCUT_BY_ID
(
    IN id INT UNSIGNED
)

BEGIN

SELECT id, url
    FROM urls.SHORTCUT
    WHERE SHORTCUT.id=id;

END //

DELIMITER ;
